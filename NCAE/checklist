Rocky Linux 8
SSH/FTP server

Intial Start:
Change Password, Change Username
sudo dnf remove -y python3 cronie

sudo chmod -R o-rwx /etc /root /bin /sbin /usr /var/log
sudo setsebool -P ftpd_disable_trans 1

lastlog | grep -v "Never logged in"
